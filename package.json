{
  "name": "flip7-scorer",
  "version": "1.0.0",
  "private": true,
  "description": "Flip7 scorer — Apps Script backend with a Node-based unit test harness.",
  "scripts": {
    "test": "node --test tests/"
  }
}
